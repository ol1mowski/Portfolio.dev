export interface StarImage {
  id: number;
  src: string;
  alt: string;
  width: number;
  height: number;
}

export interface EbookWrapperProps {
  children: React.ReactNode;
}

export interface EbookCtaWrapperProps {
  children: React.ReactNode;
}
